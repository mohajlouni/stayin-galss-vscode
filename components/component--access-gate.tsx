import React from "react";
import {
  Stack,
  usePathname,
  useRouter
} from "expo-router";
import ReactDOM from "react-dom/client";
import { useState, useEffect, useRef, useCallback, useMemo } from "react";
import { View, Text, Image, Pressable, StyleSheet, StatusBar, Platform } from "react-native";
import { Text as RNText, TextInput, TouchableOpacity, SafeAreaView, KeyboardAvoidingView, ScrollView } from "react-native";
import { StatusBar as EXPStatusBar } from "expo-status-bar";
import { AppState } from "react-native";
import { Platform } from "react-native";
import {
  ActivityIndicator,
  View
} from "react-native";
import React from "react";
import { useState } from "react";
import { useEffect } from "react";
import { useCallback } from "react";
import { View, Text, StyleSheet, TouchableOpacity, Platform } from "react-native";
import { View, Text, StyleSheet, TouchableOpacity, Platform, StatusBar, ActivityIndicator, RefreshControl } from "react-native";
import { Text, View, StyleSheet, ActivityIndicator, Platform } from "react-native";
import {
  View,
  StyleSheet,
  StatusBar,
  Platform,
  AppState
} from "react-native";
import { Router, useRouter } from "expo-router";
import { useRouter } from "expo-router";
import { Text } from "react-native";